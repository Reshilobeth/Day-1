1. HTTP2 is much faster and more reliable than HTTP1.
   HTTP1 loads a single request for every TCP connection.
   HTTP2 avoid network delay by using multiplexing.

2. In javascript, an object is a standalone entity, with properties and type.
   Compare it with a car, for example. A car is an object, with properties. a car has a color.
   a design, a perormance, etc. The same way, Javascript objects can have properties, which
   define their characteristics.